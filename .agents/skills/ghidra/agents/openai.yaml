interface:
  display_name: "Ghidra"
  short_description: "Repo-specific patterns and workflows for ghidra"
  default_prompt: "Use the ghidra repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true